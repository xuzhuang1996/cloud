eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false # ??????????
    fetchRegistry: false # ????????????
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ # defaultZone???????????????,??eureka????????????????????????????

spring:
  application:
    name: eureka-server